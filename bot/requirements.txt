# Bot Factory - Bot Dependencies
# aiogram bot for Telegram integration

# Core
aiogram>=3.0.0
python-dotenv>=1.0.0

# Django Integration (for ORM access)
asgiref>=3.7.0
Django>=5.0,<6.0

# Django REST Framework (required by INSTALLED_APPS)
djangorestframework>=3.15.0
djangorestframework-simplejwt>=5.3.0

# Django Admin & Extensions (required by INSTALLED_APPS)
django-unfold>=0.2.0
django-cors-headers>=4.3.0
django-filter>=23.3

# Django Utilities
django-environ>=0.11.0

# Database (for PostgreSQL support)
psycopg2-binary>=2.9.0

# HTTP client for API calls (optional, for direct API integration)
aiohttp>=3.9.0
httpx>=0.25.0

# Utilities
python-dateutil>=2.8.2

# Encryption (for Django token encryption compatibility)
cryptography>=41.0.0

# Image processing (for Django file handling)
Pillow>=10.0.0

